const BASE_URL = 'https://places.googleapis.com/v1/places:searchText'

// to be added...
const config = {
    headers:{
        'Content-Type': 'application/json',
        'X-Goo-Api-Key': 'API_KEY'
    }
}